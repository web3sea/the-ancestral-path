export enum Role {
  USER = "user",
  ADMIN = "admin",
}

export enum SubscriptionTier {
  TIER1 = "tier1",
  TIER2 = "tier2",
}

export enum SubscriptionStatus {
  ACTIVE = "active",
  CANCELLED = "cancelled",
  PAUSED = "paused",
  EXPIRED = "expired",
}
